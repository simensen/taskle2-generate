<?php
{% include 'DocBlock.twig' %}

namespace {{ namespace }};

interface {{ retrieveRepository.classname }}
{
    public function retrieve{{ plural | StudlyCase }}({{ criteria | parameter }});
    public function retrieve{{ singular | StudlyCase }}Count({{ criteria | parameter }});
}
