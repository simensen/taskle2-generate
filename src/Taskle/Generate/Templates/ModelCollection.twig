<?php
namespace {{ namespace }};

use Pinpoint\Shared\EntityCollection;

class {{ collection.classname }} extends EntityCollection
{
    public function add{{ entity.classname }}({{ entity | parameter }})
    {
        $this->addEntity({{ entity | variable }});
    }

    public function remove{{ entity.classname }}({{ entity | parameter }})
    {
        $this->removeEntity({{ entity | variable }});
    }
}
